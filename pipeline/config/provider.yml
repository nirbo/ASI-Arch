# Provider configuration used by the generic connector
# Replace values to point to your desired LLM provider
model:
  name: gpt-oss-20b            # default model name
  base_url: http://192.168.100.100:8080/v1   # default base URL
  api_key: dummy                 # default API key (override in env)

rag:
  url: http://localhost:13142
  k: 3
  similarity_threshold: 0.5
